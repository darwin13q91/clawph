## [2026-03-17 09:00] Scout Fix Complete - Root Cause: Wrong Script Name in Bridge

**Problem:** Scout was failing with "Browser open failed" errors and falling back to RapidAPI for ASIN B0CZXC3NJD.

**Root Cause Found:**
In `/home/darwin/.openclaw/agents/scout/scripts/echo-scout-bridge.sh`:
- Line 11 checked for: `unified-amazon-audit.sh` ✓
- Line 12 executed: `unified-audit.sh` ✗ (WRONG - missing "-amazon")

This caused the script to fail silently and fall back to RapidAPI.

**Fix Applied:**
```bash
# Before (broken):
"$SCOUT_DIR/scripts/unified-audit.sh" "$ASIN" >> "$LOG_FILE" 2>&1

# After (fixed):
"$SCOUT_DIR/scripts/unified-amazon-audit.sh" "$ASIN" >> "$LOG_FILE" 2>&1
```

**Verification:**
- Ran `bash echo-scout-bridge.sh B0CZXC3NJD test@example.com`
- Result: `{"status": "success", "source": "scout_unified"}`
- Report generated: `/home/darwin/.openclaw/agents/scout/reports/unified_audit_B0CZXC3NJD_20260317_085816.json`
- Score: 79/100 with actionable recommendations

**Next Steps:**
1. Test end-to-end via Echo → Scout → River pipeline
2. Monitor queue.log for successful Scout completions
3. Remove old failed queue entries if needed

**System:** Scout audit pipeline
**Impact:** High - Restores Scout functionality for free audit requests
